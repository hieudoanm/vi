version: 2.1

jobs:
  build:
    docker:
      - image: node:16
    working_directory: ~/vi/packages/api
    steps:
      - checkout
      - run:
          name: 'Installing Dependencies'
          command: yarn install
      - run:
          name: 'Build Production'
          command: yarn build
      - run:
          name: 'Check Files & Folder'
          command: pwd && ls -la
      - store_artifacts:
          path: ./dist
          destination: build
  test:
    docker:
      - image: node:16
    working_directory: ~/vi/packages/api
    steps:
      - checkout
      - run:
          name: 'Installing Dependencies'
          command: yarn install
      - run:
          name: 'Unit Tests'
          command: yarn test
      - run:
          name: 'Check Files & Folder'
          command: pwd && ls -la
      - store_test_results:
          path: ./coverage
          destination: tests

workflows:
  version: 2
  build_test:
    jobs:
      - build
      - test:
          requires:
            - build
